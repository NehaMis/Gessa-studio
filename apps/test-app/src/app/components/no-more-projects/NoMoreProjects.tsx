import { Paper, Typography } from '@mui/material';

const NoMoreProjects = () => {
  return (
    <div className="p-4">
      <Paper className="w-full rounded h-container flex justify-center items-center">
        <Typography variant="body1">No More Projects!</Typography>
      </Paper>
    </div>
  );
};

export default NoMoreProjects;
