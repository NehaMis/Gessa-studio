import { Typography } from '@mui/material';
import React from 'react';

const Child = () => {
  return (
    <div>
      <Typography variant="body1">I am child</Typography>
    </div>
  );
};

export default Child;
