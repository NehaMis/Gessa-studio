import { Typography } from '@mui/material';
import React from 'react';

const Parent = () => {
  return (
    <div>
      <Typography variant="body1">I am parent</Typography>
    </div>
  );
};

export default Parent;
